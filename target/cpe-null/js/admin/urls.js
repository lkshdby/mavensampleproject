define([], 
function(){
	return {
		allcluster: icasConfig.contextPath + "/rest/allclusters",
		cluster: icasConfig.contextPath + "/rest/clusters",
		subscriber: icasConfig.contextPath + "/rest/subscribers"
	};
});
