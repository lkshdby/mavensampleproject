ALTER TABLE GATEWAYS DROP FOREIGN KEY GATEWAYS_ibfk_1;
ALTER TABLE GATEWAYS DROP COLUMN subscriberId;
ALTER TABLE GATEWAYS ADD COLUMN accountId VARCHAR(36);
ALTER TABLE GATEWAYS ADD CONSTRAINT `GATEWAYS_ibfk_1` FOREIGN KEY (accountId) REFERENCES ACCOUNTS(id) ON DELETE CASCADE;

ALTER TABLE VPNTUNNELS DROP FOREIGN KEY VPNTUNNELS_ibfk_2;
ALTER TABLE VPNTUNNELS DROP COLUMN clusterId;

DELETE FROM VPNTUNNEL_SUBNET;
DROP TABLE VPNTUNNEL_SUBNET;
CREATE TABLE SOFTLAYERORDERS (
  id VARCHAR(50),
  softLayerId INT,
  clusterId VARCHAR(36),
  tierName VARCHAR(100),
  softLayerAccountId VARCHAR(36),
  PRIMARY KEY (id),
  FOREIGN KEY (clusterId) REFERENCES CLUSTERS(id) ON DELETE CASCADE,
  FOREIGN KEY (softLayerAccountId) REFERENCES SOFTLAYERACCOUNTS(id) ON DELETE CASCADE
);
